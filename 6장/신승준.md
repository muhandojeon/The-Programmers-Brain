# 코딩 문제 해결을 더 잘하려면

## 정신 모델

트리 순회, 파일 시스템과 같이 사실은 0과 1로 저장된 데이터일뿐, 실제로 그렇게 존재하진 않는다.

하지만 이러한 정신 모델을 바탕으로 코드를 추론하는 것이 더 쉽다.

### 정신 모델의 특징

두뇌에서 많은 양의 에너지를 필요로 하기에 가급적이면 정신 모델을 사용하지 않으려고 한다.

ex. 디버깅 시 코드를 조금만 고친 후 버그가 해결되었는지 계속 확인함
